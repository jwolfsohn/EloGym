<!DOCTYPE html>
<html>
  <head>
    <title>EloGym - Gamify Your Fitness</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="min-h-screen flex flex-col">
    <% unless controller_name.in?(['sessions', 'registrations']) || (controller_name == 'onboarding' && action_name.in?(['welcome', 'split', 'stats'])) %>
      <!-- Top Navigation -->
      <nav class="bg-zinc-800 border-b border-zinc-700">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
          <div class="flex justify-between items-center h-16">
            <div class="flex items-center">
              <h1 class="text-2xl font-bold text-brand-purple">EloGym</h1>
            </div>

            <% if logged_in? %>
              <div class="flex items-center gap-4">
                <div class="flex items-center gap-2">
                  <span class="rank-badge rank-<%= current_user.rank %>"><%= current_user.rank %></span>
                  <div class="text-sm">
                    <p class="font-semibold"><%= current_user.username %></p>
                    <p class="text-gray-400"><%= current_user.total_points %> pts</p>
                  </div>
                </div>
                <%= link_to user_path(current_user), class: "text-gray-300 hover:text-brand-purple transition-colors" do %>
                  <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z" />
                  </svg>
                <% end %>
              </div>
            <% end %>
          </div>
        </div>
      </nav>
    <% end %>

    <!-- Flash Messages -->
    <% if flash.any? %>
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-4">
        <% flash.each do |type, message| %>
          <div class="<%= type == 'notice' ? 'bg-green-900/50 border-green-700' : 'bg-red-900/50 border-red-700' %> border rounded-lg p-4 mb-4">
            <p class="<%= type == 'notice' ? 'text-green-200' : 'text-red-200' %>"><%= message %></p>
          </div>
        <% end %>
      </div>
    <% end %>

    <!-- Main Content -->
    <main class="flex-1 pb-24">
      <%= yield %>
    </main>

    <% unless controller_name.in?(['sessions', 'registrations']) || (controller_name == 'onboarding' && action_name.in?(['welcome', 'split', 'stats'])) %>
      <!-- Bottom Navigation -->
      <nav class="fixed bottom-0 left-0 right-0 bg-zinc-800 border-t border-zinc-700">
        <div class="max-w-7xl mx-auto px-4">
          <div class="flex justify-around items-center h-16">
            <%= link_to dashboard_path, class: "nav-link #{'active' if controller_name == 'dashboard'}" do %>
              <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M3 12l2-2m0 0l7-7 7 7M5 10v10a1 1 0 001 1h3m10-11l2 2m-2-2v10a1 1 0 01-1 1h-3m-6 0a1 1 0 001-1v-4a1 1 0 011-1h2a1 1 0 011 1v4a1 1 0 001 1m-6 0h6" />
              </svg>
              <span class="text-xs mt-1">Dashboard</span>
            <% end %>

            <%= link_to battles_path, class: "nav-link #{'active' if controller_name == 'battles'}" do %>
              <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M11.049 2.927c.3-.921 1.603-.921 1.902 0l1.519 4.674a1 1 0 00.95.69h4.915c.969 0 1.371 1.24.588 1.81l-3.976 2.888a1 1 0 00-.363 1.118l1.518 4.674c.3.922-.755 1.688-1.538 1.118l-3.976-2.888a1 1 0 00-1.176 0l-3.976 2.888c-.783.57-1.838-.197-1.538-1.118l1.518-4.674a1 1 0 00-.363-1.118l-3.976-2.888c-.784-.57-.38-1.81.588-1.81h4.914a1 1 0 00.951-.69l1.519-4.674z" />
              </svg>
              <span class="text-xs mt-1">Battles</span>
            <% end %>

            <% if logged_in? %>
              <%= link_to user_path(current_user), class: "nav-link #{'active' if controller_name == 'users' && action_name == 'show'}" do %>
                <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z" />
                </svg>
                <span class="text-xs mt-1">Profile</span>
              <% end %>
            <% end %>
          </div>
        </div>
      </nav>
    <% end %>
  </body>
</html>
